# Module 2 Labs — Core Coding Concepts

```
l2-1-greeting-routine.sb3   2.1 — a timed sequence (go to 0,0 · say · wait · move · say)
l2-2-walking-cat.sb3        2.2 — repeat 20: move 20, next costume, wait 0.1 (a real walk cycle)
l2-3-arrow-key-cat.sb3      2.3 — forever + four "if key … pressed?" checks (drive the cat with the arrow keys)
```

**2.3 tip:** keyboard control only works after you click the stage once, so it has focus.
Do not hand out reference files before the lab.
